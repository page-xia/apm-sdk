{
  "name": "root",
  "private": false,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.6.1"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "publish:pkg": "npm run build && lerna publish from-package"
  },
  "config": {
    "dsnUrl": "https://aiapi.nestsound.cn/querymodeltype"
  },
  "version": "0.0.0"
}
